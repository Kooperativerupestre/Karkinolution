FROM ubuntu:25.04

ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update && apt-get install -y \
    build-essential \
    clang \
    cmake \
    ninja-build \
    valgrind \
    git \
    ca-certificates \
    libc6-dbg \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /workspace