The Karkinolution CLI should be used for style verification and code formatting.

To activate the CLI, run:

```bash
uv sync

source .venv/bin/activate
```

`uv` is preferred in this project for dependency management.
